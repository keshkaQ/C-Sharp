
//using System.Text;

//// Создаем объект StringBuilder для эффективного построения строки
//// (в отличие от конкатенации строк, не создает множество промежуточных объектов)
//var stringBuilder = new StringBuilder();

//// Цикл выполняется 1000 раз (O(n), где n = 1000)
//for (int i = 0; i < 1000; i++)
//{
//    // Преобразуем текущее число i в строку и добавляем в StringBuilder
//    stringBuilder.Append(i.ToString());

//    /* Как это работает внутри:
//    1. Выделяет буфер памяти (по умолчанию 16 символов)
//    2. При заполнении буфера автоматически увеличивает его размер
//    3. Хранит символы в массиве char[]
//    */
//}

//// После завершения цикла преобразуем содержимое StringBuilder в строку и выводим
//// ToString() здесь возвращает все добавленные символы как единую строку
//Console.WriteLine(stringBuilder.ToString());